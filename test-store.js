const store = require("./dispatch.store");

console.log(store.getState());

// const data = store.getState().contacts;

// data.map(contact =>
//   console.log("Name: " + contact.name + " Phone: " + contact.phone)
// );
